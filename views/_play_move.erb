<ul class="small-block-grid-3">
  <li class="panel text-center">
    <form action="/api/players/<%= @player.player_id %>/matches/<%= @match.match_id %>/games/<%= @game.game_id %>" method="post">
      <input name="action" type="hidden" value="paper">
      <button type="submit" class="callout panel text-center">
        ROCK
      </button>
    </form>
  </li>
  <li class="panel text-center">
    <form action="/api/players/<%= @player.player_id %>/matches/<%= @match.match_id %>/games/<%= @game.game_id %>" method="post">
      <input name="action" type="hidden" value="paper">
      <button type="submit" class="callout panel text-center">
        PAPER
      </button>
    </form>
  </li>
  <li class="panel text-center">
    <form action="/api/players/<%= @player.player_id %>/matches/<%= @match.match_id %>/games/<%= @game.game_id %>" method="post">
      <input name="action" type="hidden" value="paper">
      <button type="submit" class="panel text-center">
        SCISSORS
      </button>
    </form>
  </li>
</ul>
