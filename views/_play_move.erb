<div class="button-bar">
<ul class="button-group round">
  <li>
    <form action="/api/players/<%= @player.player_id %>/matches/<%= @match.match_id %>/games/<%= @game.game_id %>" method="post">
      <input name="action" type="hidden" value="rock">
      <button type="submit" class="button large alert">
          </br>ROCK</br></br>
      </button>
    </form>
  </li>
  <li>
    <form action="/api/players/<%= @player.player_id %>/matches/<%= @match.match_id %>/games/<%= @game.game_id %>" method="post">
      <input name="action" type="hidden" value="paper">
      <button type="submit" class="button large alert">
        </br>PAPER</br></br>
      </button>
    </form>
  </li>
  <li>
    <form action="/api/players/<%= @player.player_id %>/matches/<%= @match.match_id %>/games/<%= @game.game_id %>" method="post">
      <input name="action" type="hidden" value="scissors">
      <button type="submit" class="button large alert">
        </br>SCISSORS</br></br>
      </button>
    </form>
  </li>
</ul>
</div>
